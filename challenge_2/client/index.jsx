// Start building your app using a single,
// fixed currency symbol, Bitcoin (BTC),
// and a fixed date range of your choosing.

// Use a time-series chart and show closing prices only.
// include the text, "Powered by CoinDesk" beneath chart

import React from 'react';
import ReactDOM from 'react-dom';
import App from './App.jsx';


ReactDOM.render(<App />, document.getElementById('app'));
